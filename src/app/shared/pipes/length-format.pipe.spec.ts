import { LengthFormatPipe } from './length-format.pipe';

describe('LengthFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
